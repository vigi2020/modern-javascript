export function AddNumbers(x, y) {
  return x + y;
}

export function SubtractNumbers(bigNumber, smallNumber) {
  return bigNumber - smallNumber;
}

//let util = { AddNumbers: AddNumbers, SubtractNumbers: SubtractNumbers };
//let util = { AddNumbers, SubtractNumbers };

//export default util;
export default { AddNumbers, SubtractNumbers };
